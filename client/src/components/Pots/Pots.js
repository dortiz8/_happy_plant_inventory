import React from 'react';
import { Button, Typography } from '@mui/material';
const Pots = () => {
    return (
        <div>
            <Typography>Pots Component</Typography>
            <Button>Add a Pot</Button>
        </div>
    )
}

export default Pots;