import React from 'react';
import { Button, Typography } from '@mui/material';
const Home = () => {
    return (
        <div>
            <Typography>Home Component</Typography>
            <Button>Add Plants</Button>
            <Button>Add Pots</Button>
        </div>
    )
}

export default Home;